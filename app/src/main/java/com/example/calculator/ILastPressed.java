package com.example.calculator;

import androidx.appcompat.app.AppCompatActivity;

interface ILastPressed {
    
    String build();
    void setAppCompactActivity(AppCompatActivity activity);

    void setText(String text);
}
